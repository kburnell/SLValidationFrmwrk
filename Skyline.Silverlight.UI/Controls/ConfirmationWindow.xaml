<controls:ChildWindow x:Class="Skyline.Silverlight.UI.Controls.ConfirmationWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
            Loaded="ChildWindow_Loaded"
           Width="400" 
           Title="Message">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" HorizontalAlignment="Left" Margin="0,7,0,0" Grid.Row="1" />
       
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" HorizontalAlignment="Right" Margin="0,7,0,0" Grid.Row="1" />
        
        <ScrollViewer BorderThickness="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="300" Grid.Row="0">
            <TextBlock x:Name="TextBlockMessage" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </ScrollViewer>
        
    </Grid>
</controls:ChildWindow>